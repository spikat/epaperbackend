<style>
.view { display:flex; flex-direction:column; height:100%; box-sizing:border-box; overflow:hidden; }
.weather-header { display:flex; justify-content:space-between; align-items:baseline; margin-bottom:4px; flex-shrink:0; }
.weather-current {
  display:flex;
  gap:12px;
  align-items:center;
  flex-shrink:0;
  margin-bottom:4px;
}
.weather-current .weather-icon { width:60px; height:60px; flex-shrink:0; }
.weather-current .value { font-size:38px; font-weight:700; line-height:1; flex:1; }
.weather-current .current-meta {
  margin-left:auto;
  text-align:right;
  font-size:16px;
  line-height:1.3;
  color:#000;
  flex:0 0 auto;
  min-width:150px;
  white-space:nowrap;
}
.title { font-size:20px; font-weight:700; color:#000; }
.meta-line { font-size:15px; color:#000; }
.chart {
  display:block;
  width:100%;
  flex: 0 0 auto;
  margin:2px 0 6px;
  overflow:hidden;
  line-height:0;
}
.chart svg { display:block; width:100%; height:auto; }
.daily-stack { display:flex; flex-direction:column; gap:4px; margin-top:auto; flex-shrink:0; }
.daily-row { display:flex; gap:4px; margin-top:0; flex-shrink:0; }
.daily-item { flex:1; min-width:0; text-align:center; border:1px solid #000; padding:5px 2px; box-sizing:border-box; font-size:15px; color:#000; }
.daily-item .weather-icon { width:38px; height:38px; display:block; margin:0 auto 2px; }
.daily-item .day-name { font-weight:700; font-size:16px; text-transform:capitalize; line-height:1.15; }
.daily-item .temps { font-size:15px; font-weight:700; line-height:1.2; margin-top:2px; }
.daily-item .wind { font-size:14px; color:#000; line-height:1.2; }
.daily-item .humidity { font-size:14px; color:#000; line-height:1.2; }

/* Half vertical: push forecast down, slightly larger type */
.view--half_vertical .title { font-size: 22px; margin-bottom: 6px; }
.view--half_vertical .weather-current { margin-bottom: 0; }
.view--half_vertical .weather-current .value { font-size: 42px; }
.view--half_vertical .weather-current .weather-icon { width: 68px; height: 68px; }
.view--half_vertical .weather-current .current-meta { font-size: 17px; min-width: 160px; }
.view--half_vertical .daily-stack {
  margin-top: auto;
  margin-bottom: 2px;
  gap: 6px;
  padding-top: 18px;
}
.view--half_vertical .daily-item { font-size: 16px; padding: 6px 3px; }
.view--half_vertical .daily-item .weather-icon { width: 42px; height: 42px; }
.view--half_vertical .daily-item .day-name { font-size: 17px; }
.view--half_vertical .daily-item .temps { font-size: 16px; }
.view--half_vertical .daily-item .wind,
.view--half_vertical .daily-item .humidity { font-size: 15px; }

/* Quadrant: fill the tile, less empty space */
.view--quadrant { padding: 6px; }
.view--quadrant .quadrant-fill {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-evenly;
  text-align: center;
  box-sizing: border-box;
}
.view--quadrant .q-title {
  font-size: 18px;
  font-weight: 700;
  line-height: 1.15;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.view--quadrant .q-icon {
  width: 88px;
  height: 88px;
  flex-shrink: 0;
}
.view--quadrant .q-temp {
  font-size: 52px;
  font-weight: 700;
  line-height: 1;
}
.view--quadrant .q-meta {
  font-size: 16px;
  line-height: 1.3;
  color: #000;
  font-weight: 600;
}
</style>
