<style>
.view { display:flex; flex-direction:column; height:100%; box-sizing:border-box; overflow:hidden; }
.weather-header { display:flex; justify-content:space-between; align-items:baseline; margin-bottom:4px; flex-shrink:0; }
.weather-current {
  display:flex;
  gap:12px;
  align-items:center;
  flex-shrink:0;
  margin-bottom:4px;
}
.weather-current .weather-icon { width:60px; height:60px; flex-shrink:0; }
.weather-current .value { font-size:38px; font-weight:700; line-height:1; flex:1; }
.weather-current .current-meta {
  margin-left:auto;
  text-align:right;
  font-size:16px;
  line-height:1.3;
  color:#111;
  flex:0 0 auto;
  min-width:150px;
  white-space:nowrap;
}
.title { font-size:20px; font-weight:700; }
.meta-line { font-size:15px; color:#666; }
.chart {
  display:block;
  width:100%;
  flex: 0 0 auto;
  margin:2px 0 6px;
  overflow:hidden;
  line-height:0;
}
.chart svg { display:block; width:100%; height:auto; }
.daily-row { display:flex; gap:4px; margin-top:auto; flex-shrink:0; }
.daily-item { flex:1; min-width:0; text-align:center; border:1px solid #ccc; padding:5px 2px; box-sizing:border-box; font-size:15px; }
.daily-item .weather-icon { width:38px; height:38px; display:block; margin:0 auto 2px; }
.daily-item .day-name { font-weight:700; font-size:16px; text-transform:capitalize; line-height:1.15; }
.daily-item .temps { font-size:15px; font-weight:700; line-height:1.2; margin-top:2px; }
.daily-item .wind { font-size:14px; color:#444; line-height:1.2; }
.daily-item .humidity { font-size:14px; color:#444; line-height:1.2; }
</style>
